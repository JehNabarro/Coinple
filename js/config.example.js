/* ── Coinple config example ──
 * Copia este ficheiro para js/config.js e preenche o teu GOOGLE_CLIENT_ID.
 * O ficheiro js/config.js está no .gitignore para evitar que as tuas credenciais
 * sejam enviadas para o repositório do GitHub (ou possíveis forks).
 */
const CONFIG = {
  GOOGLE_CLIENT_ID: 'O_TEU_GOOGLE_CLIENT_ID.apps.googleusercontent.com',
};
